#include <iostream>
#include <fstream>
#include <map>
#include <string>

using namespace std;

int main () {

    std::map<std::string, int> traffic;

    std::ifstream in("210-final-1-FA25.txt");
    if (!in) {
        cout << "Error opening file." << endl;
        return 1;
    }

    std::string origin;
    std;;sting destination;

    while

    return 0;
}